[project]
name = "gymops"
version = "0.1.0"
description = "GymOps — gym management system"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.1,<6.2",
    "psycopg[binary]>=3.3.4",
]
